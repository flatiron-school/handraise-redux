<% unless @current_user == asker %>

  <% if issue.voted_on?(@current_user) %>
    <%= link_to ("-" + issue.votes.size.to_s), votedown_path %>
  <% else %>
    <%= link_to ("+" + issue.votes.size.to_s), voteup_path %>
  <% end %>

<% end %>