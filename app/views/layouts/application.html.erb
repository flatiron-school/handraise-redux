<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Hand Raise" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- For third-generation iPad with high-resolution Retina display: -->
    <!-- Size should be 144 x 144 pixels -->
    <%#= favicon_link_tag 'images/apple-touch-icon-144x144-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '144x144' %>

    <!-- For iPhone with high-resolution Retina display: -->
    <!-- Size should be 114 x 114 pixels -->
    <%#= favicon_link_tag 'images/apple-touch-icon-114x114-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '114x114' %>

    <!-- For first- and second-generation iPad: -->
    <!-- Size should be 72 x 72 pixels -->
    <%#= favicon_link_tag 'images/apple-touch-icon-72x72-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '72x72' %>

    <!-- For non-Retina iPhone, iPod Touch, and Android 2.1+ devices: -->
    <!-- Size should be 57 x 57 pixels -->
    <%#= favicon_link_tag 'images/apple-touch-icon-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png' %>

    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <%#= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>

    <% if current_page?(issues_path) || current_page?(big_board_path) %>
      <meta http-equiv="refresh" content="60">
    <% end %>
  </head>
  <body>

    <div class="navbar navbar-fluid-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="brand" href="/">HandRaise</a>
          <div class="container-fluid nav-collapse">
            <ul class="nav">
              <li><%= link_to "View All Issues", issues_path %></li>
              <li><%= link_to "View Big Board", big_board_path %></li>
              <li><%= link_to "Add An Issue", new_issue_path %></li> 
              <li><%= link_to "Report A Bug", "https://github.com/flatiron-school/handraise-redux/issues", :target => "_blank" %></li>
            </ul>
            <ul class="login">
              <li>
                <% if current_user %>
                  Logged in as: <%= link_to current_user.email, user_path(current_user) %>
                  <button class="btn btn-small btn-primary"><%= link_to "Logout", logout_path %></button>
                <% else %>
                  <% unless current_page?(new_user_path) %>
                    <button class="btn btn-small btn-success"><%= link_to("Sign Up", new_user_path) %></button>
                  <% end %>
                  <% unless current_page?(login_path) %>
                    <button class="btn btn-small btn-primary"><%= link_to("Login", login_path) %></button> 
                  <% end %>
                  <%#= link_to("Login with Github", "https://github.com/login/oauth/authorize?client_id=#") %>
                <% end %> 
              </li>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container-fluid" id="content">
      <div class="row-fluid">
        <% unless current_page?(big_board_path) %>
          <div  class="box span3">
            <div class="box-header">
              <h2><i class="icon-tasks"></i><span class="break"></span>Issues Stats</h2>
            </div>
            <div class="box-content">
              <p><strong>Current open issues:</strong>
                <%= Issue.total_open_issues %></p>
              
              <p><strong>Average waiting time for open issues:</strong>
                <%= Issue.average_wait_time_open.to_i %> minutes</p>
                  
              <hr>

              <p><strong>Total closed issues: </strong>
                <%= Issue.total_closed_issues %></p>
              
              <% if Issue.total_closed_issues > 0 %>
              <p><strong>Average time taken to resolve issues:</strong>
                <%= Issue.average_wait_time_closed.to_i %> minutes</p>                            
              <% end %>
            </div>
          </div><!--/.box -->
        <% end %>
        
        <% if current_page?(big_board_path) %>
        <div class="span12">
        <% else %>
        <div class="span9">
        <% end %>
          <%= bootstrap_flash %>

            <%= yield %>
        </div>
      </div><!--/row-->


    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->

    <%= javascript_include_tag "application" %>

  </body>
</html>
