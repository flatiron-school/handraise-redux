<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Hand Raise" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- For third-generation iPad with high-resolution Retina display: -->
    <!-- Size should be 144 x 144 pixels -->
    <%#= favicon_link_tag 'images/apple-touch-icon-144x144-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '144x144' %>

    <!-- For iPhone with high-resolution Retina display: -->
    <!-- Size should be 114 x 114 pixels -->
    <%#= favicon_link_tag 'images/apple-touch-icon-114x114-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '114x114' %>

    <!-- For first- and second-generation iPad: -->
    <!-- Size should be 72 x 72 pixels -->
    <%#= favicon_link_tag 'images/apple-touch-icon-72x72-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '72x72' %>

    <!-- For non-Retina iPhone, iPod Touch, and Android 2.1+ devices: -->
    <!-- Size should be 57 x 57 pixels -->
    <%#= favicon_link_tag 'images/apple-touch-icon-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png' %>

    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <%#= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>

    <% if current_page?(issues_path) || current_page?(big_board_path) %>
      <meta http-equiv="refresh" content="60">
    <% end %>
  </head>
  <body>

    <div class="navbar navbar-fluid-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <%= link_to "HandRaise", issues_path, :class => 'brand' %>
          <div class="container-fluid nav-collapse">
            <ul class="nav">
              <li><%= link_to "View All Issues", issues_path %></li>
              <li><%= link_to "View Big Board", big_board_path %></li>
              <li><%= link_to "Add An Issue", new_issue_path %></li>
              <% if current_user && current_user.admin? %>  
              <% end %>
              <li><%= link_to 'Admin Checkin', indexadmin_path %></li> 
              <li><%= link_to "Report A Bug", "https://github.com/flatiron-school/handraise-redux/issues", :target => "_blank" %></li>
            </ul>
            <ul class="login">
              <li>
                <% if current_user %>
                  Logged in as: <%= link_to current_user.email, user_path(current_user) %>
                  <%= link_to "Logout", logout_path %>
                <% else %>
                  <% unless current_page?(new_user_path) %>
                    <%= link_to("Sign Up", new_user_path) %> 
                  <% end %>
                  <% unless current_page?(login_path) %>
                    <%= link_to("Login", login_path) %> 
                  <% end %>
                  <%#= link_to("Login with Github", "https://github.com/login/oauth/authorize?client_id=#") %>
                <% end %> 
              </li>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container-fluid">
      <div class="row-fluid">
        <% unless current_page?(big_board_path) %>
        <div class="span3">
          <div class="well sidebar-nav">
            <ul class="nav nav-list">
              <li class="nav-header">Issues summary</li>
              <li>Current open issues:<br>
                <strong><%= Issue.total_open_issues %></strong>
              </li>
              <li>Average waiting time for open issues:<br>
                <strong><%= Issue.average_wait_time_open.to_i %> minutes</strong>
              </li>    
              <hr>
              <li>Total closed issues: <br> 
                <strong><%= Issue.total_closed_issues %></strong>
              </li>
              <% if Issue.total_closed_issues > 0 %>
              <li>Average time taken to resolve issues:<br>
                <strong><%= Issue.average_wait_time_closed.to_i %> minutes</strong>
              </li>              
              <% end %>
            </ul>
          </div><!--/.well -->
        </div><!--/span-->
        <% end %>
        
        <% if current_page?(big_board_path) %>
        <div class="span12">
        <% else %>
        <div class="span9">
        <% end %>
          <%= bootstrap_flash %>

            <%= yield %>
        </div>
      </div><!--/row-->

      <footer>
        <p>&copy; Company 2013</p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->

    <%= javascript_include_tag "application" %>

  </body>
</html>
